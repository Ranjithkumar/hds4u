.block
  .content
    %h2.title= t('views.users.new_user')
    .inner
      - form_for @user, :url => users_path, :html => {:class => "form"} do |f|
        .columns.wat-cf
          = error_messages_for :user
          = render :partial => "form", :locals => {:f => f}
