%tr
  %td= group.id
  %td= group.title
  %td.last
    =link_to t('web-app-theme.edit'), edit_group_url(group)
    = "|"
    =link_to t('web-app-theme.delete'), group_url(group), :method => :delete, :confirm => "Are you sure?"
